<% @pages.each do |page| %>
  <%= page.title %>
  <%= page.date %>
  <%= page.description %>
  <%= image_tag page.images.first.image_url(width: 300, height: 300, format: 'jpg', fl: 'progressive') %>
<% end %>
